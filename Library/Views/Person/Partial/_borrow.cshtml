@model Library.Models.Person

    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)


    <fieldset>
        <legend>Wählen Sie ein Buch</legend>
        <div class="editor">
            @Html.Label("Book")
            @Html.DropDownList("Borrowed", String.Empty)
        </div>
    </fieldset>
